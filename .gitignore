# env (실값/시크릿 — git 에 안 올림, RULES §4.1. .env.example 만 올림)
.env
backend/.env

# 하네스 인프라 (add-cto 설치 — 모듈 deliverable 아님, git 제외. 디스크엔 유지)
.claude/

# harness 전용 compose (공유 mediamtx 로 도는 호스트에서만 사용 — clone 사용자 무관. 디스크엔 유지)
docker-compose.harness.yml

# 내부 빌드 입력/기록 (공개 불필요) — specs 전체(USAGE.md 포함) + plan.md
plan.md
specs/

# python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# DB (경로 무관 — 글로벌)
*.db
*.sqlite
*.sqlite3

# node
node_modules/
dist/

# misc
.DS_Store
*.log
.gstack/

# host dev runner (./dev.sh)
.dev.pids
.dev-logs/
